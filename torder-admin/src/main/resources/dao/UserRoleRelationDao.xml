<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="club.mrdaisite.torder.torderadmin.dao.UserRoleRelationDao">
    <select id="getPermissionList" resultType="club.mrdaisite.torder.tordermbg.model.Permission">
        select p.*
        from user_role_relation ur
                 left join role r on ur.role_id = r.id
                 left join role_permission_relation rp on r.id = rp.role_id
                 left join permission p on rp.permission_id = p.id
        where ur.user_id = #{userId};
    </select>
</mapper>